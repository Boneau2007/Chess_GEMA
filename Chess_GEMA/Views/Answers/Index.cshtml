@model IEnumerable<Chess_GEMA.Models.Answer>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PositivRating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NegativeRating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StarAnswer)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PositivRating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NegativeRating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StarAnswer)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
